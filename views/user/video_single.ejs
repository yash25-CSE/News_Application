<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title><%= video.headline %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body { background: #eef2f5; }
        .container-custom {
            background: white;
            padding: 25px;
            border-radius: 12px;
            box-shadow: 0 3px 10px rgba(0,0,0,0.1);
        }
        iframe {
            width: 100%;
            height: 400px;
            border-radius: 12px;
        }
        .tags span {
            background: #007bff;
            color: white;
            padding: 5px 10px;
            border-radius: 20px;
            margin-right: 5px;
            font-size: 12px;
        }
    </style>

</head>
<body>

<div class="container py-4">

    <div class="container-custom">

        <h2 class="fw-bold"><%= video.headline %></h2>

        <div class="mt-3">
            <iframe 
                src="<%= video.videoUrl.replace('watch?v=', 'embed/') %>" 
                allowfullscreen>
            </iframe>
        </div>

        <div class="mt-4">
            <p><%= video.article %></p>
        </div>

        <div class="tags mt-3">
            <% if (video.tags.length > 0) { %>
                <% video.tags.forEach(t => { %>
                    <span>#<%= t %></span>
                <% }) %>
            <% } %>
        </div>

        <a href="/videos" class="btn btn-dark mt-4">⬅ Back to All Videos</a>

    </div>

</div>

</body>
</html>
