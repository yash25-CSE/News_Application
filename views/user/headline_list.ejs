<!DOCTYPE html>
<html>
<head>
    <title>Latest Headlines</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body class="bg-light">

<div class="container mt-5">
    <h2 class="mb-4 text-primary">Latest Headlines</h2>

    <div class="row">
        <% headlines.forEach(h => { %>
            <div class="col-md-4 mb-4">
                <div class="card shadow">
                    <% if (h.imageUrl) { %>
                        <img src="<%= h.imageUrl %>" class="card-img-top" alt="Headline">
                    <% } %>

                    <div class="card-body">
                        <h5 class="card-title"><%= h.title %></h5>
                        <p class="text-muted"><%= h.summary %></p>
                        <small class="text-secondary">
                            <i class="bi bi-clock"></i> <%= h.createdAt.toDateString() %>
                        </small>
                    </div>
                </div>
            </div>
        <% }) %>
    </div>
</div>

</body>
</html>
