<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Samachar App Navbar</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        /* Custom styling for a subtle, creative shadow/border effect */
        .creative-nav {
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05), 0 2px 4px -2px rgba(0, 0, 0, 0.05);
            border-bottom: 1px solid #f3f4f6; /* Light gray border for separation */
        }
    </style>
</head>
<body>

    <nav class="creative-nav w-full bg-white fixed top-0 z-50 mb-5">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 ">
            <div class="flex justify-center h-24">
                <div class="flex items-center">
                    <a href="#" class="flex-shrink-0">
                        <img class="h-24 w-auto" src="/uploads/images/logo.png" alt="Samachar App Logo">
                    </a>
                </div>

                <div class="absolute right-4 top-0 h-full flex items-center">
                    <button class="text-gray-600 hover:text-red-600 p-2 rounded-full transition duration-300">
                        <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
                        </svg>
                    </button>
                </div>
            </div>
        </div>
    </nav>



    <script>

    document.addEventListener('DOMContentLoaded', function() {
        const nav = document.querySelector('.creative-nav');
        
        // Function to add/remove a stronger shadow on scroll
        function handleScroll() {
            if (window.scrollY > 50) {
                // Add a slightly stronger shadow class after scrolling 50px
                nav.classList.add('shadow-md');
            } else {
                nav.classList.remove('shadow-md');
            }
        }

        // Listen for scroll events
        window.addEventListener('scroll', handleScroll);
    });

    </script>

</body>
</html>